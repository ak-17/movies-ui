import { StringLengthManagePipe } from './string-length-manage.pipe';

describe('StringLengthManagePipe', () => {
  it('create an instance', () => {
    const pipe = new StringLengthManagePipe();
    expect(pipe).toBeTruthy();
  });
});
